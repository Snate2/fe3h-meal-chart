import { SelectedCharacterPipe } from './selected-character.pipe';

describe('SelectedCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
